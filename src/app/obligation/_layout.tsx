import { Stack } from "expo-router";

export default function ObligationLayout() {
  return (
    <Stack
      screenOptions={{
        headerStyle: { backgroundColor: "#121220" },
        headerTintColor: "#fff",
        headerTitleStyle: { fontWeight: "600" },
      }}
    />
  );
}
